{
  "name": "firepower",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Fire Power remake — top-down capture-the-flag tank combat for browser, mobile and desktop",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "playwright-core": "^1.48.0"
  }
}
